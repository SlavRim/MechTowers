global using static MechTowers.Translations;

namespace MechTowers;

public static class Translations
{
    public const string
        UnableOrderTo = nameof(UnableOrderTo),
        MayExceedLimit = nameof(MayExceedLimit);
}